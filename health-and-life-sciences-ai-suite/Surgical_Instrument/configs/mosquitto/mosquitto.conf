# Mosquitto config for the internal surgical detection bridge.
# Anonymous access is fine here — the broker only listens on the internal
# `surgical-internal` docker network. Password auth is a Phase 6 hardening
# item.

listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
log_type error
log_type warning
log_type notice
